{{EDITOR_SYSTEM_PROMPT}}

你现在执行的是“知识星球 PDF 批量总结”的本地摘要阶段。

你的职责只有一件事：
- 读取当前批次已经恢复好的正文文本
- 按上面的摘要规则生成本地摘要
- 返回机器可解析结果

禁止做的事：
- 不要创建飞书文档
- 不要追加飞书文档
- 不要调用任何飞书工具
- 不要自行读取 PDF
- 不要自行做 OCR

当前批次文件清单：
- 批次文件：`{{BATCH_JSON_PATH}}`
- 当前是第 `{{CHUNK_INDEX}}` 批，共 `{{CHUNK_TOTAL}}` 批
- 当前分批文件数：`{{CHUNK_FILE_COUNT}}`
- 本轮总文件数：`{{TOTAL_FILE_COUNT}}`

当前分批必须处理的文件就是下面这些，禁止增删改，禁止替换成同目录其他 PDF。`优先读取文本` 是唯一主输入：
{{CURRENT_FILE_MANIFEST}}

当前分批允许返回的 `handled_paths` 只能是下面这个 JSON 数组，必须逐字复制，不得替换、不得遗漏、不得加入清单外路径：
`{{CURRENT_PATH_JSON}}`

执行顺序必须严格遵守：
1. 先读取 `{{BATCH_JSON_PATH}}`
2. 只处理 `files` 数组里的文件
3. 每个文件都必须先读取 `优先读取文本`
4. 如果缺少 `优先读取文本`、文本为空，或批次 JSON 已写明 `text_extract_error`，直接失败
5. 不要写飞书，不要假装发布成功

单个 PDF 的摘要结构必须严格如下：

# <中文标题>
> 原始文件名：<文件名>

## 核心结论
- <第1条>
- <第2条>
- <按原文信息量继续写>

## 核心问题与回答

### 1. <核心问题>
<按问题复杂度回答；信息量大时可以写长一些>

### 2. <核心问题>
<按问题复杂度回答；信息量大时可以写长一些>

### 3. <核心问题>
<按问题复杂度回答；信息量大时可以写长一些>

补充规则：
- 不要默认写正文质量声明
- 核心结论按信息量写，一般 3-8 条；长篇深度报告可以更多，但每条都要有新增信息
- 核心问题按信息量写，一般 3-8 个；长篇深度报告可以更多，但不要为了凑数重复
- 每个回答按报告复杂度调整长度；普通问题简洁回答，复杂问题可以写更长，把关键数据、原因、影响和风险讲完整
- 一级标题必须是中文标题
- 不得使用表格、emoji、分割线
- 不得复述免责声明，不得大段照抄原文
- 不要给判断、预测额外加免责声明式解释；用自然语气保留原文观点即可
- 不要为了说明缺少信息而套固定句式；按原文已有信息自然总结

输出要求：
- 先给一段简洁完成通知
- 最后单独输出一行机器可读结果
- 机器可读结果必须是一行 JSON，`markdown` 字段里用 JSON 字符串保存完整 Markdown，换行要转义

成功时必须严格输出：

【ZSXQ PDF 本地摘要完成】
处理数量：<当前这一个分批处理完成的数量>
文件：
- <文件名1>
- <文件名2>

ZSXQ_SUMMARY_JSON: {"status":"success","handled_count":<n>,"handled_paths":{{CURRENT_PATH_JSON}},"summaries":[{"path":"<文件路径>","filename":"<文件名>","title":"<中文标题>","quality_hint":"<没有就留空>","markdown":"<完整 markdown，必须是 JSON 字符串>"}]}

失败时必须严格输出：

【ZSXQ PDF 本地摘要失败】
原因：<失败原因>

ZSXQ_SUMMARY_JSON: {"status":"failed","handled_count":0,"handled_paths":[],"summaries":[],"error":"<失败原因>"}
