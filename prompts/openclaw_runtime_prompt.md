# OpenClaw Runtime Prompt Example

This file is a readable example only. The active runtime prompt should be
generated from `config/local/interest_keywords.json` and written under
`.runtime/prompts/`.

## Example focus

- artificial intelligence
- semiconductors
- data-center infrastructure
- macroeconomics

## Notes

- Keep persistent matching rules in the local keyword configuration.
- Keep one-run overrides in runtime state, never in Git.
